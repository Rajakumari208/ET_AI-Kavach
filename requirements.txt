scikit-learn>=1.3
opencv-python-headless>=4.8
numpy>=1.24
pandas>=2.0
streamlit>=1.30
pydeck>=0.8

# Optional - only needed if using the live WhatsApp alert provider
# instead of the default local DummyProvider
# twilio>=8.10
